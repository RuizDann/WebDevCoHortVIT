/*
Apply onclick to each button to:
    - make the box bigger
    - return the box to normal
    - make the box smaller
*/

function getBigger() {
    var big = document.getElementById('ourBox').setAttribute('class' , 'getBigger');
    
}
function getNormal() {
    var normal = document.getElementById('ourBox').setAttribute('class' , 'getNormal');
}
function getSmaller() { 
    var small = document.getElementById('ourBox').setAttribute('class' , 'getSmaller');
}